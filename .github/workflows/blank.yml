name: OWASP ZAP Scan

on:
  pull_request:
    branches:
      - main  # Trigger ZAP scan on PRs targeting the `main` branch
  push:
    branches:
      - main  # Or trigger on push to the `main` branch
  workflow_dispatch:  # Allow manual triggering of the workflow

permissions:
  contents: write  # Required for the ZAP scan to work correctly

jobs:
  zap_scan:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the repository
      - name: Checkout Repository
        uses: actions/checkout@v3

      # Step 2: Set up Python (if required for your application)
      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: '3.8'
          
      # Step 3: Run OWASP ZAP Scan
      - name: ZAP Scan
        uses: zaproxy/action-baseline@v0.15.0
        with:
          target: 'https://www.medium.com/'  # Replace with your target URL
          token: ${{ secrets.GITHUB_TOKEN }}
          
